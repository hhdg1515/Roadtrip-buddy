import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      eyebrow="Loading comparison"
      title="Building the side-by-side deck"
      description="The shortlist cards are still resolving live conditions, fit scoring, and risk comparisons."
    />
  );
}
